package recursions;

public class sumOfDigits {
	static int digits(int n) {
		if(n == 0) {
			return n;
		}
		int rem = 0;
		rem = rem + (n % 10);
		digits(n / 10);
		return rem;
	}
public static void main(String[] args) {
	System.out.println(digts(1342));
}
}
